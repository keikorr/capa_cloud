node_modules/
cloudflared.exe
*.log
.DS_Store
Thumbs.db
.env
.env.*
!.env.example

# Fixture reconstruída da produção para os golden snapshots.
# Contém credenciais Cielo por totem (a API as devolve para CRPADMIN) — nunca versionar.
data/fixture_prod.json
data/*.bak-*
